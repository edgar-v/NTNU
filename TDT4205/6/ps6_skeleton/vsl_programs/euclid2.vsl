FUNC gcd( a, b )
BEGIN
    VAR g
    g := 0
    WHILE g < 10 DO
    BEGIN
        g := g + 1
        IF g > 5 THEN
        BEGIN
            CONTINUE
        END
        PRINT g
    END
    RETURN g
END
